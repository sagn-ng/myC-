#include <iostream> //header file library to work with input and output objects
using namespace std;
int main(){
    cout<<"hello world\nSang";
    //cout: an object used together with the insertion operator "<<" to output/print text
    //sometimes: "using namespace std" is omitted, and there will be "std::" for some objects (like "cout")
    return 0;
}